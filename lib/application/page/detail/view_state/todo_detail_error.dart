import 'package:flutter/material.dart';

class ToDoDetailError extends StatelessWidget {
  const ToDoDetailError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text("Detail頁面錯誤，請稍後再試"),);
  }
}
